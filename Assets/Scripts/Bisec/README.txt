In order to use the Bisec tool, add the "bMesh" script to your GameObject. The GameObject MUST CONTAIN a MeshRenderer.

For collision:
 - GameObject must use a MeshCollider for it to update with the mesh properly
 - other colliders will work, but will not update accordingly